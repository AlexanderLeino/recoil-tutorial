import { selector, get } from 'recoil'
import textState from './atoms'
const charCountState = selector({
        key:'charCountState',
        get: ({get}) => {
            console.log(get)
            const text = get(textState)
            
            return text.length
        }
    })

export default charCountState